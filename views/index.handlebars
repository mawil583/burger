<div class="container">
    <div class="row">
        <div class="col-6">
            <h1>Burgers to eat</h1>
            <ul>
                {{#each burger}}
                <li>Burger Name: {{burger_name}}</li>
                {{!-- <li>Has been eaten (1 = true, 0 = false): {{devoured}}</li> --}}
                <form action="/burgers/{{id}}" method="GET">
                <button>Devour</button>
                    
                </form>
                {{/each}}
            </ul>
        </div>
        <div class="col-6">
            <h1>Burgers eaten:</h1>

        </div>
    </div>
</div>






<form action="/api/burgers" method="post">

    <div class="form-group">
        <label>Burger Name:</label>
        <input type="text" name="burger_name">

    </div>
    <button type="submit">Add Burger</button>
</form>